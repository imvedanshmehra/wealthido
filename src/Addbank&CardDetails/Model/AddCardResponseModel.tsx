export type AddCardResponseModel = {
  brand?: string;
  complete?: boolean;
  expiryMonth?: number;
  expiryYear?: number;
  last4?: string;
  validCVC?: string;
  validExpiryDate?: string;
  validNumber?: string;
};
